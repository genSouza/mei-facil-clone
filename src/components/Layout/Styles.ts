import { styled } from "@/styles/index";

export const LayoutWrapper = styled("div", {
  maxWidth: "90rem",
  margin: "0 auto",
  boxShadow: "0 0 25px 1px rgb(0 0 0 / 10%)",
  height: "calc(100% - 44.375rem)",
  paddingTop: "80px",
});
